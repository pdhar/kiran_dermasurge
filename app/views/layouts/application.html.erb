<!DOCTYPE html>
<html>
	<head>
		<title><%= full_title(yield(:title)) %></title>
		<%= metamagic :description => "We at Kiran Dermasurge strive to provide highest level of medically proved skin treatments and therapies. Our state of art clinic is equipped with the latest laser technology and the most advanced skin care treatments. ", 
			:keywords => "skin, laser treatment, hair removal, acne, photo facial, weight loss, vitiligo, dermatology, skin clinic, dubai, gurgaon, delhi, ncr" %>
		
		<%= favicon_link_tag 'favicon.ico' %>
		<%= stylesheet_link_tag "http://ajax.googleapis.com/ajax/libs/jqueryui/1.9.2/themes/base/jquery-ui.css" %>
		<%= stylesheet_link_tag    "application", media: "all" %>
		<link href='http://fonts.googleapis.com/css?family=Josefin+Sans:400,100' rel='stylesheet' type='text/css'>
		<link href='http://fonts.googleapis.com/css?family=Scada' rel='stylesheet' type='text/css'>
		<%= javascript_include_tag "application"%>
		<%= javascript_include_tag :defaults, "http://pdhar-faye-server.herokuapp.com/faye.js" %>

		<%= csrf_meta_tags %>
		<%= render 'layouts/shim' %>
		<script>
			( function(d, s, id) {
					var js, fjs = d.getElementsByTagName(s)[0];
					if (d.getElementById(id))
						return;
					js = d.createElement(s);
					js.id = id;
					js.src = "//connect.facebook.net/en_US/all.js#xfbml=1&appId=493272794050268";
					fjs.parentNode.insertBefore(js, fjs);
				}(document, 'script', 'facebook-jssdk'));
		</script>

		<script type="text/javascript">
			$(window).load(function() {
				$("#sticker").sticky({
					topSpacing : 90
				});
			});
		</script>

		<script type="text/javascript">
		  var _gaq = _gaq || [];
		  _gaq.push(['_setAccount', 'UA-38778547-1']);
		  _gaq.push(['_setDomainName', document.domain]);
		  _gaq.push(['_setAllowLinker', true]);
		  _gaq.push(['_trackPageview']);
		
		  (function() {
		    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
		    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
		    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
		  })();		
		</script>
		
		<script>
		  $(function() {
		    var name = $( "#name" ),
		      email = $( "#email" ),
		      //password = $( "#password" ),
		      allFields = $( [] ).add( name ).add( email ), //.add( password ),
		      tips = $( ".validateTips" );
		 	
		 	box = null;
		 
		    function updateTips( t ) {
		      tips
		        .text( t )
		        .addClass( "ui-state-highlight" );
		      setTimeout(function() {
		        tips.removeClass( "ui-state-highlight", 1500 );
		      }, 500 );
		    }
		 
		    function checkLength( o, n, min, max ) {
		      if ( o.val().length > max || o.val().length < min ) {
		        o.addClass( "ui-state-error" );
		        updateTips( "Length of " + n + " must be between " +
		          min + " and " + max + "." );
		        return false;
		      } else {
		        return true;
		      }
		    }
		 
		    function checkRegexp( o, regexp, n ) {
		      if ( !( regexp.test( o.val() ) ) ) {
		        o.addClass( "ui-state-error" );
		        updateTips( n );
		        return false;
		      } else {
		        return true;
		      }
		    }
		 
		    $( "#dialog-form" ).dialog({
		      autoOpen: false,
		      height: 300,
		      width: 350,
		      modal: true,
		      buttons: {
		        "Start Chat": function() {
		          var bValid = true;
		          allFields.removeClass( "ui-state-error" );
		 
		          bValid = bValid && checkLength( name, "username", 3, 16 );
		          bValid = bValid && checkLength( email, "email", 6, 80 );
		          //bValid = bValid && checkLength( password, "password", 5, 16 );
		 
		          bValid = bValid && checkRegexp( name, /^[a-z]([0-9a-z_])+$/i, "Name may consist of a-z, 0-9, underscores, begin with a letter." );
		          // From jquery.validate.js (by joern), contributed by Scott Gonzalez: http://projects.scottsplayground.com/email_address_validation/
		          bValid = bValid && checkRegexp( email, /^((([a-z]|\d|[!#\$%&'\*\+\-\/=\?\^_`{\|}~]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])+(\.([a-z]|\d|[!#\$%&'\*\+\-\/=\?\^_`{\|}~]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])+)*)|((\x22)((((\x20|\x09)*(\x0d\x0a))?(\x20|\x09)+)?(([\x01-\x08\x0b\x0c\x0e-\x1f\x7f]|\x21|[\x23-\x5b]|[\x5d-\x7e]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(\\([\x01-\x09\x0b\x0c\x0d-\x7f]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]))))*(((\x20|\x09)*(\x0d\x0a))?(\x20|\x09)+)?(\x22)))@((([a-z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(([a-z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])*([a-z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])))\.)+(([a-z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(([a-z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])*([a-z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])))\.?$/i, "eg. ui@jquery.com" );
		          //bValid = bValid && checkRegexp( password, /^([0-9a-zA-Z])+$/, "Password field only allow : a-z 0-9" );
		 
		 		/*	
		          if ( bValid ) {
		            $( "#users tbody" ).append( "<tr>" +
		              "<td>" + name.val() + "</td>" +
		              "<td>" + email.val() + "</td>" +
		              "<td>" + password.val() + "</td>" +
		            "</tr>" );
		            $( this ).dialog( "close" );
		          }
		          */
		         //alert('email' + email.val());
		         
		         if ( bValid ) {
			          if (box) {
							box.chatbox("option", "boxManager").toggleBox();
					  } else {
							box = $("#chat_div").chatbox({
								id : "chat_div",
								user  : name.val(),	
								email : email.val(),
								title : "Live Chat"
							});
							
					  }
					  
					  $( this ).dialog( "close" );
					}	  	
		        },
		        Cancel: function() {
		          $( this ).dialog( "close" );
		        }
		      },
		      close: function() {
		        allFields.val( "" ).removeClass( "ui-state-error" );
		      }
		    });
		 
		    $( "#create-user" )
		      .button()
		      .click(function() {
		      	if (!box) {
							$( "#dialog-form" ).dialog("option","height",350).dialog("option","width",400).dialog( "open");
					  }else
					  {
					  	box.chatbox("option", "boxManager").toggleBox();
					  }
		        
		      });
		  });
	  </script>
		  
	</head>
	<body style="position: relative;">

		<div id="fb-root"></div>

		<%= render 'layouts/header' %>
		
		<div id="wrapper" >
			<a id="create-user" href="#" onclick="this.href='javascript:return false;';">
				<div id="sticker">
				</div>
			 </a>
		</div>
		<div class="container">
			<!-- hello  url_for(:controller=>'static_pages', :action=>'home', :only_path =>false )  -->
			<% flash.each do |key, value| %>
			<div class="alert alert-<%= key %>">
				<%= value %>
			</div>

			<% end %>

			<%= render 'layouts/locations' %>

			<!--div class="modal hide fade in" data-backdrop="static" id="RegistrationEditModal">
			<div class="modal-header">
			<a class="close" data-dismiss="modal">×</a>
			<h3>This is a Modal Heading</h3>
			</div>
			<div class="modal-body">
			#render 'devise/registrations/edit'
			</div>
			<div class="modal-footer">
			<a href="#" class="btn btn-success">Call to action</a>
			<a href="#" class="btn" data-dismiss="modal">Close</a>
			</div>
			</div>

			<a class="btn" data-toggle="modal" href="#user-edit" >Launch Modal</a-->

			<%= yield %>

			<!--input id="chatbutton" type="button"
			name="toggle" value="toggle" /-->
			<div style="display: none">
				<div id="chat_div" style="width: 250px;"></div>
				<hr />
				<div id="log"></div>
			</div>
			
			<div id="dialog-form" title="Create chat user">
			  <p class="validateTips">Please enter your details.</p>
			  <form>
				  <fieldset>
				    <label for="name">Name</label>
				    <input type="text" name="name" id="name" class="text ui-widget-content ui-corner-all" />
				    <label for="email">Email</label>
				    <input type="text" name="email" id="email" value="" class="text ui-widget-content ui-corner-all" />
				  </fieldset>
			  </form>
			</div>
			
			<!--input id="test_chat" type="button"
			name="test_chat" value="test_chat" /-->

		<div style="background-color: rgba(92,92,158,0.95)" class="navbar navbar-fixed-bottom">
			<div class="container">
				<%= render 'layouts/footer' %>
			</div>
		</div>

	</body>
</html>